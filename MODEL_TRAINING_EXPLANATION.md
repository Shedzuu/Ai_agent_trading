# Объяснение обучения модели

## 📊 Как работает обучение модели

### Текущая реализация (обновлена)

Теперь модель может обучаться **на реальных исторических данных**, которые мы получаем через Market Monitoring Agent!

---

## 🔄 Два режима обучения

### 1. Обучение на исторических данных (РЕКОМЕНДУЕТСЯ) ✅

**По умолчанию включено:** `use_historical_training=True`

**Как работает:**
1. Decision Making Agent получает исторические данные через Market Monitoring Agent
2. Для каждого дня извлекаются признаки (технические индикаторы)
3. Метка (label) определяется на основе **будущего движения цены**:
   - Если цена выросла > 1% и RSI не перекуплен → BUY (2)
   - Если цена упала > 1% и RSI не перепродан → SELL (0)
   - Иначе → HOLD (1)
4. Модель обучается на этих реальных примерах

**Пример использования:**
```python
from agents.decision_maker import DecisionMakingAgent

# Обучение на исторических данных AAPL за 1 год
agent = DecisionMakingAgent(
    model_type="random_forest",
    use_historical_training=True,  # Использовать реальные данные
    training_ticker="AAPL",         # Тикер для обучения
    training_period="1y"            # Период: 1y, 6mo, 3mo, 1mo
)

# При первом вызове модель автоматически обучится
decision = agent.receive_market_data(market_data)
```

**Преимущества:**
- ✅ Модель учится на реальных рыночных данных
- ✅ Учитывает реальные паттерны рынка
- ✅ Более точные предсказания

---

### 2. Обучение на синтетических данных (Fallback)

**Используется если:**
- `use_historical_training=False`
- Не удалось получить исторические данные
- Недостаточно исторических данных (< 100 записей)

**Как работает:**
1. Генерирует 1000 случайных примеров
2. Применяет правило-основанную логику для создания меток:
   - Если тренд бычий + RSI < 40 + MACD > 0 → BUY
   - Если тренд медвежий + RSI > 60 + MACD < 0 → SELL
   - Иначе → HOLD
3. Модель обучается на этих синтетических примерах

**Пример:**
```python
agent = DecisionMakingAgent(
    use_historical_training=False  # Использовать синтетические данные
)
```

---

## 📈 Процесс обучения на исторических данных

### Шаг 1: Получение данных

```python
# Market Monitoring Agent получает исторические данные
market_agent = MarketMonitoringAgent(
    ticker="AAPL",      # или "SPY" для широкого рынка
    interval="1d",      # Дневные данные
    period="1y"         # За 1 год
)

data = market_agent.get_processed_data(analyze=False)
# Получаем DataFrame с индикаторами
```

### Шаг 2: Извлечение признаков

Для каждого дня извлекаются **14 признаков**:

1. **Ценовые:**
   - close (цена закрытия)
   - volume (объем)
   - price_change (изменение цены)

2. **Технические индикаторы:**
   - sma10, sma20 (скользящие средние)
   - rsi14 (индекс относительной силы)
   - macd, macd_hist (MACD индикатор)
   - volatility (волатильность)

3. **Анализ:**
   - trend_encoded (тренд: bull=1.0, bear=-1.0, sideways=0.0)
   - strength (сила тренда)
   - rsi_state (overbought/oversold/neutral)
   - sma_cross (пересечение SMA)

### Шаг 3: Создание меток (Labels)

Метка определяется на основе **будущего движения цены**:

```python
# Для дня i смотрим цену дня i+1
current_price = data.iloc[i]['close']
next_price = data.iloc[i+1]['close']
price_change_pct = ((next_price - current_price) / current_price) * 100

# Определение метки
if price_change_pct > 1.0 and rsi < 60:
    label = 2  # BUY - цена выросла, не перекуплен
elif price_change_pct < -1.0 and rsi > 40:
    label = 0  # SELL - цена упала, не перепродан
else:
    label = 1  # HOLD - нейтральная ситуация
```

**Логика:**
- Если цена выросла > 1% и рынок не перекуплен → это был хороший момент для покупки
- Если цена упала > 1% и рынок не перепродан → это был хороший момент для продажи
- Иначе → лучше было держать

### Шаг 4: Обучение модели

```python
# Разделение данных
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

# Нормализация
X_train_scaled = scaler.fit_transform(X_train)

# Обучение
model = RandomForestClassifier(n_estimators=100)
model.fit(X_train_scaled, y_train)

# Оценка
accuracy = model.score(X_test_scaled, y_test)
```

---

## 🎯 Рекомендации по использованию

### Для лучших результатов:

1. **Используйте исторические данные:**
   ```python
   DecisionMakingAgent(use_historical_training=True)
   ```

2. **Выберите подходящий тикер:**
   - Для конкретной акции: `training_ticker="AAPL"`
   - Для широкого рынка: `training_ticker="SPY"` (по умолчанию)

3. **Выберите период обучения:**
   - `"1y"` - 1 год (рекомендуется)
   - `"6mo"` - 6 месяцев
   - `"3mo"` - 3 месяца
   - `"1mo"` - 1 месяц (минимум)

4. **Сохраняйте обученную модель:**
   ```python
   agent = DecisionMakingAgent(
       model_path="./models/trading_model.pkl"
   )
   # Модель сохранится после обучения
   ```

---

## 📊 Сравнение подходов

| Характеристика | Исторические данные | Синтетические данные |
|---------------|---------------------|---------------------|
| **Реалистичность** | ✅ Реальные паттерны | ⚠️ Идеализированные |
| **Точность** | ✅ Выше | ⚠️ Ниже |
| **Скорость** | ⚠️ Медленнее (загрузка данных) | ✅ Быстрее |
| **Требования** | ✅ Нужен интернет | ✅ Не нужен |
| **Размер данных** | ✅ Много (сотни записей) | ⚠️ Мало (1000 синтетических) |

---

## 🔍 Как проверить, что модель обучается на исторических данных

### Вариант 1: Проверка логов

При обучении вы увидите:
```
INFO: Attempting to train on real historical data...
INFO: Fetching historical data for SPY (period: 1y)
INFO: Prepared 250 training samples from historical data
INFO: Label distribution: BUY=85, HOLD=120, SELL=45
INFO: Model trained. Train accuracy: 0.750, Test accuracy: 0.720
```

### Вариант 2: Проверка в коде

```python
from agents.decision_maker import DecisionMakingAgent
from agents.market_monitor import MarketMonitoringAgent

# Создать агента с историческим обучением
agent = DecisionMakingAgent(
    use_historical_training=True,
    training_ticker="AAPL",
    training_period="1y"
)

# Получить данные
market_agent = MarketMonitoringAgent(ticker="AAPL")
market_data = market_agent.send_to_decision_agent()

# Принять решение (модель обучится автоматически)
decision = agent.receive_market_data(market_data)

print(f"Модель: {decision['model_type']}")
# Если обучена на исторических данных, будет "random_forest" или "gradient_boosting"
```

---

## ⚙️ Технические детали

### Структура данных для обучения

**Входные данные (X):**
- Массив формы `(n_samples, 14)`
- 14 признаков для каждого примера

**Выходные данные (y):**
- Массив формы `(n_samples,)`
- Метки: 0 (SELL), 1 (HOLD), 2 (BUY)

### Модели

1. **Random Forest:**
   - 100 деревьев
   - Максимальная глубина: 10
   - Случайное состояние: 42 (для воспроизводимости)

2. **Gradient Boosting:**
   - 100 итераций
   - Максимальная глубина: 5
   - Случайное состояние: 42

### Нормализация

Все признаки нормализуются через `StandardScaler`:
- Среднее = 0
- Стандартное отклонение = 1

Это необходимо для корректной работы моделей.

---

## 🚀 Пример полного использования

```python
from agents.decision_maker import DecisionMakingAgent
from agents.market_monitor import MarketMonitoringAgent

# Создать Decision Agent с обучением на исторических данных
decision_agent = DecisionMakingAgent(
    model_type="random_forest",
    use_historical_training=True,  # ✅ Использовать реальные данные
    training_ticker="AAPL",         # Обучить на данных AAPL
    training_period="1y",           # За последний год
    model_path="./models/aapl_model.pkl"  # Сохранить модель
)

# Получить текущие данные рынка
market_agent = MarketMonitoringAgent(ticker="AAPL")
market_data = market_agent.send_to_decision_agent()

# Принять решение (модель обучится при первом вызове)
decision = decision_agent.receive_market_data(market_data)

print(f"Решение: {decision['action']}")
print(f"Уверенность: {decision['confidence']:.2f}")
print(f"Модель обучена на: исторических данных AAPL за 1 год")
```

---

## ✅ Итог

**Теперь модель обучается на реальных исторических данных!**

- ✅ Получает данные через Market Monitoring Agent
- ✅ Извлекает признаки из реальных рыночных данных
- ✅ Создает метки на основе будущего движения цены
- ✅ Обучает модель на реальных паттернах
- ✅ Fallback на синтетические данные если нужно

**Это делает модель более точной и реалистичной!** 🎯

